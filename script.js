const mostrarDetalles = (nombreProyecto) => {
    const mensaje = `Has seleccionado el proyecto: ${nombreProyecto}`;
    alert(mensaje);
};

// Declaracion basica
function mostrarDetalles(nombreProyecto) {
    const mensaje = `Has seleccionado el proyecto: ${nombreProyecto}`;
    alert(mensaje);
}